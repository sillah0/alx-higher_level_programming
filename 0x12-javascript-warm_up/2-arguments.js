#!/usr/bin/node

const numargs = process.argv.slice(2);
if (numargs === 0) {
  console.log('No argument');
} else if (numargs === 1) {
  console.log('Argument found');
} else {
  console.log('Argument found');
}
